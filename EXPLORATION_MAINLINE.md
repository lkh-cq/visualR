# visualR 新探索主线：分布式残差拓扑运行时

> 状态：可运行的探索假设，不是冻结公理、论文结论或生产规范。
> 分支目标：把 2026-08-09 对“局部涌现—中枢路由—局部同谐”的讨论，
> 编译成第一组可以运行、反驳和继续改写的 R 对象。

## 1. 方向改变

此前的 visualR 主线回答了一个问题：PAL 如何展开成并发轨道，经过算子后再闭合回
PAL。它已经具备“同一快照、多轨并发、统一提交”的骨架，但轨道仍主要由回文壳层
给出。

新主线追问另一件事：

> 当信息存在于一个有位置、有余量的残差场中，多个局部如何同时从不同位置取样，
> 又不挤占同一节点、不过度抽取，并把取样位置之间的关系作为结果的一部分留下？

因此，新内核不是固定的 A/B/C/D/e 轨道表，而是动态的：

```text
残差场快照
  -> 多个局部管口产生位置偏好
  -> 中枢只分配取样权
  -> 所有管口同时获得分配
  -> 输出局部信号 + 取样拓扑
  -> 原子扣减残差
  -> 相位步进，进入下一轮
```

PAL 不被删除。它退到一个可选拓扑来源：可以生成节点、包含关系或局部地址，但不再
预先规定所有运行时都必须是某个有限字母切片。

## 2. 最小对象

残差场写作：

$$
R_t=\{(s_j,p_j,q_j,c_j)\}_{j=1}^{N}
$$

- \(s_j\)：节点信号；
- \(p_j\)：节点位置；
- \(q_j\)：尚未抽取的残差供给；
- \(c_j\)：该节点在一个时刻允许被抽取的上限。

第 \(i\) 个局部管口只向中枢暴露：

$$
P_i=(id_i,b_i,\phi_i,\alpha_i,h_i)
$$

- \(b_i\)：本轮流量预算；
- \(\phi_i\)：当前位置相位；
- \(\alpha_i\)：相位步长；
- \(h_i\)：空间取样带宽。

管口内部怎样理解信号，不向路由器开放。中枢因此是逻辑中心，而不是认知中心。

## 3. 联合分配

路由阶段先生成非负偏好矩阵 \(W=[w_{ij}]\)。分配阶段求取
\(A=[a_{ij}]\)，满足：

$$
\sum_j a_{ij}\le b_i
$$

$$
\sum_i a_{ij}\le \min(q_j,c_j)
$$

当前 R 原型采用同步 water-filling：每轮所有管口都根据同一个剩余快照提出请求；
某节点请求过量时，该节点上的所有请求按同一比例缩放。随后才统一进入下一轮。
它不是全局最优求解器，而是一个顺序无偏、可审计的最小联合分配语义。

提交时检查：

$$
\sum_j q_j^t
=
\sum_{ij}a_{ij}
+
\sum_j q_j^{t+1}
+\varepsilon_t
$$

当前实现要求 \(\varepsilon_t\) 在数值容差内，否则中止提交。

## 4. 位置不是附属元数据

每轮产物不是单个融合向量，而是：

$$
O_t=(\{z_i\},T_t,R_{t+1})
$$

- \(z_i\)：每个局部得到的抽取量、信号和位置质心；
- \(T_t\)：被取样节点、管口—节点支持关系、节点间距离和方向差；
- \(R_{t+1}\)：提交后的残差场。

单个坐标只是地址；多个地址之间的距离、方向和共同取样关系才进入拓扑输出。

## 5. 无理数暂时承担什么

无理数目前只进入可装卸的相位调度策略：

$$
\phi_i^{t+1}=(\phi_i^t+\alpha_i)\bmod 1
$$

在理想连续系统中，有理步长形成周期，无理步长不精确闭合。实现使用双精度浮点，
所以这里只把它视为“减少短周期重复的可执行近似”，不把浮点序列冒充无理性的证明。

无理信息未来也可能属于递归轨道极限、边界或高维投影；本分支不提前冻结这三者。

## 6. 当前 API

```r
field <- new_reservoir(
  signal,
  position,
  supply,
  capacity
)

pipes <- list(
  new_reservoir_pipe("local-a", budget = 0.8, phase = 0),
  new_reservoir_pipe("local-b", budget = 0.8, phase = 0.5)
)

result <- reservoir_step(field, pipes, k = 4)
```

一个步进结果同时包含：

- `preferences`：局部位置偏好；
- `allocation`：联合取水矩阵与约束余量；
- `outputs`：各局部的抽取结果；
- `topology`：节点、边和管口支持关系；
- `reservoir_out`：下一时刻残差场；
- `pipes_out`：下一相位的局部管口；
- `conservation`：供给守恒记录。

## 7. 尚未替用户决定的部分

本分支有意不实现一个“通用同谐评分器”。两个内部不可见的局部凭什么能够同谐，
仍需要新的最小边界定义。当前 `outputs + topology` 是可路由边界包，而不是已经完成
的同谐判决。

同样未冻结：

- 高维残差究竟是额外坐标轴、递归轨道还是多个局部场的耦合；
- 节点供给应怎样从真实神经残差或其他载体中定义；
- 位置拓扑在学习中怎样反向影响下一轮路由；
- PAL 与 reservoir 之间的双向变换规则；
- 稠密偏好、稀疏近邻和未来硬件并行之间的性能分界。

## 8. 下一轮探索

1. 用不同相位步长运行多轮轨迹，比较覆盖、碰撞与残差耗散，而不是预设无理步长
   一定更好。
2. 为 `outputs + topology` 设计最小 boundary packet，再由调用者注入同谐算子。
3. 让一次成功同谐产生局部 delta，并仅改写参与交互的两个局部。
4. 设计 PAL ↔ reservoir adapter，让回文成为一种生成/压缩透镜。
5. 只有 profiling 证明联合分配是热点后，再讨论 C++、Java 或 GPU 执行织物。

这条主线的判断标准不是“是否像一个成熟神经网络”，而是：一次局部涌现如何发生、
中枢究竟看见了什么、资源在哪里发生碰撞、拓扑有没有被压没，以及下一步能否由本步
继续推导。
